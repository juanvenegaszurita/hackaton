import 'package:flutter/material.dart';

class TableResponsive extends StatelessWidget {
  TableResponsive({
    required this.children,
  });
  final List<List<dynamic>> children;

  @override
  Widget build(BuildContext context) {
    
    return Column(
      children: [],
    );
  }
}
